# Slide 17: sensitive values. TEMPLATE ONLY.
# Never commit real secret values. Create at apply-time instead, e.g.:
#   kubectl create secret generic inference-api-secret \
#     --from-literal=MODEL_API_KEY='<your-real-key>' -n ai-workloads
apiVersion: v1
kind: Secret
metadata:
  name: inference-api-secret
type: Opaque
stringData:
  MODEL_API_KEY: "REPLACE_ME_do_not_commit_a_real_key"
